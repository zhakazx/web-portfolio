{
  "buildCommand": "tanstack build",
  "outputDirectory": ".tanstack",
  "installCommand": "npm install",
  "builds": [
    { "src": "server.mjs", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "server.mjs" }
  ]
}
